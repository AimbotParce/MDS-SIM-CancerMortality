name: Knit MD on Push

env:
  RMD_FILE: CancerMortality.Rmd
  TARGET_MD: README.md

on:
  push:
    branches:
      - main
    paths:
      - CancerMortality.Rmd
      - .github/workflows/knit-md-on-push.yaml # This file (for dev purposes)

jobs:
  knit-md:
    
    permissions:
      contents: write
      actions: write
      
    runs-on: windows-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v2
      with:
        token: ${{ secrets.GITHUB_TOKEN }}
        lfs: true

    - name: Install R
      uses: r-lib/actions/setup-r@v2
      with:
        r-version: '4.4.1'

    - name: Install dependencies
      run: Rscript .github/scripts/install_packages.R

    - name: Install Pandoc
      run: |
        choco install pandoc -y
        pandoc --version

    - name: Remove old $TARGET_MD
      run: rm $TARGET_MD

    - name: Render Rmd to md
      run: Rscript -e 'rmarkdown::render("$RMD_FILE", output_format = "md_document", output_file = "$TARGET_MD")'

    - name: Commit changes
      run: |
        git config --global user.name 'github-actions[bot]'
        git config --global user.email 'github-actions[bot]@users.noreply.github.com'
        git add $TARGET_MD
        git add images/*
        git commit -m 'Update $TARGET_MD with contents of $RMD_FILE'
        git push